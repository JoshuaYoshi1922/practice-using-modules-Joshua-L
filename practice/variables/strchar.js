const readline = require('readline-sync');
let myStr = readline.question("What happened with MIZZOU? ");
let numBrac = readline.question("What is phrase[20] in the statement above?");
console.log(myStr);
console.log(numBrac); //20